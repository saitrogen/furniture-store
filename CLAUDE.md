Act as my expert frontend developer. We are completely rewriting the UI for my furniture e-commerce website on a fresh branch.
The Goal: We are building a lean MVP based on a specific, premium design.
Our Strict Development Rules:

- Component-by-Component: We will build this iteratively. Do not try to write the entire page or multiple components at once. Wait for my approval before moving to the next piece.
- No Hardcoded Data: The UI components must be completely dumb and driven by props(or similer).
- Local Data Sourcing
- Styling: We are using Tailwind CSS. Keep classes clean and modular. skills are avaliable at .agents/skills folder ,load which one u want or add it to .claude/skills in the project (only whats will be nessory on the long run, keep the one time using skills in the .agents/skills file for context save)