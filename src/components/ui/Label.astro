---
interface Props {
  text: string;
  class?: string;
}
const { text, class: className = '' } = Astro.props;
---

<span class:list={[
  'text-[10px] font-semibold tracking-[0.18em] uppercase text-muted-foreground',
  className
]}>
  {text}
</span>